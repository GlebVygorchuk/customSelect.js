# ⚡customSelect.js

Легковесный, доступный кастомный селект для современных веб-приложений


# ДЕМО 
Откройте и запустите файл demo.html и опробуйте сами!


# 🎯 Возможности

- 🚀 Легковесный - Всего 9 КБ, нулевые зависимости
- ♿ Доступный - Полная поддержка WAI-ARIA и клавиатурной навигации
- 🎨 Кастомизируемый - Легко стилизуется через CSS
- 🛠 Простой API - Начало работы менее чем за 5 минут
- 📱 Адаптивный - Работает на всех современных устройствах
- 🎪 Гибкий - Размещайте где угодно в вашем макете


# 📦 Установка

// Скопируйте файлы customSelect.js и customSelect.css в папку вашего проекта


# 🚀 Быстрый старт

- Базовое использование (простой массив опций):

new CustomSelect(['Яблоко', 'Банан', 'Апельсин'], 'container')

- Расширенная конфигурация(с плейсхолдером и кастомным контейнером): 

new CustomSelect({
    options: ['Москва', 'СПб', 'Казань'],
    placeholder: 'Выберите город...'
}, 'myContainer')


# 📖 Документация

// Параметры конструктора: 

new CustomSelect(config, container)
1. config - обязательный параметр, массив опций или объект конфигурации
2. container - опциональный параметр, принимает в себя id контейнера или DOM элемент,
в который будет вставлен селект


// Объект конфигурации
{
   options: ['Опция 1', 'Опция 2', 'Опция 3'],   // Обязательно
   placeholder: 'Выберите...'                    // Опционально, по умолчанию 'Options...'
}


# 💡 Примеры

- Несколько селектов в разных контейнерах
// В конкретных контейнерах
new CustomSelect(['Опция A', 'Опция B'], 'leftSidebar')
new CustomSelect(['Опция X', 'Опция Y'], 'rightSidebar')

// Прямо в DOM-элементе
const myDiv = document.getElementById('custom-container')
new CustomSelect(['Один', 'Два', 'Три'], myDiv)

- Работа с событиями
const select = new CustomSelect({
    options: ['Красный', 'Зеленый', 'Синий'],
    placeholder: 'Выберите цвет...'
}, 'colorSelect')

// Следите за изменениями
select.on('change', (selectedValue) => {
    console.log('Выбрано:', selectedValue)
    document.getElementById('result').textContent = selectedValue
})

- Методы
const select = new CustomSelect(['A', 'B', 'C'])

// Получить текущее значение
const value = select.getValue()
console.log(value.text) // Возвращает выбранный текст

// Установить значение программно
select.setValue(1) // Выбирает опцию по индексу 1

// Уничтожить экземпляр
select.destroy()


# 🎨 Кастомизация

// Кастомные стили

.select-button {
    background-color: ваш-цвет;
    border: 1px solid ваша-рамка;
    font-family: ваш-шрифт;
}

.select-options {
    background-color: цвет-выпадающего-списка;
    box-shadow: ваша-тень;
}


# ⌨️ Клавиатурная навигация

1. Enter/Пробел - Открыть/закрыть выпадающий список, выбрать опцию
2. Стрелка Вверх/Вниз - Навигация по опциям
3. Escape - Закрыть выпадающий список
4. Home/End - Перейти к первой/последней опции
5. Tab - Закрыть список и перейти к следующему элементу


# 📞 Справочник API

Методы: 

1. getValue() - Возвращает { text: string }
2. setValue(index) - Выбирает опцию по индексу
3. destroy() - Удаляет экземпляр селекта
4. on(event, callback) - Подписка на события
5. trigger(event, data) - Вызов кастомных событий

События:

change - Срабатывает при выборе опции


## 🤝 Поддержать проект

**89822912669 - Т-Банк**







